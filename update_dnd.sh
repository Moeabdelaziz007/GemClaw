#!/bin/bash
# Replaces react-dnd with framer-motion drag in SpatialCanvas

# For drag-and-drop UI components (e.g., SpatialCanvas), prefer using `framer-motion` over `react-dnd` to avoid Next.js build module resolution errors and React ref mismatches.
